= title(t('pages.checks'))

= t('modules.checks.text')

= subtitle(t('modules.checks.variables.variables'))
%p
=  t('modules.checks.variables.text')

= attribute_table(t('modules.checks.variables.variables'), false, [t('table.name'), t('table.description'), t('table.information')]) do
  = attribute_row do
    = attribute_name("Attacker")
    = attribute_desc(t('modules.checks.variables.attacker-desc'))
    = attribute_type(:player)
  = attribute_row do
    = attribute_name('Damage')
    = attribute_desc(t('modules.checks.variables.damage-desc'))
    = attribute_desc(label('Damage Cause')) do
      = link_to('Damage Types' ,'https://hub.spigotmc.org/javadocs/spigot/org/bukkit/event/entity/EntityDamageEvent.DamageCause.html')
  = attribute_row do
    = attribute_name('Entity')
    = attribute_desc(t('modules.checks.variables.entity-desc'))
    = attribute_desc(label('Entity Type')) do
      = link_to('Entity Types' ,'https://hub.spigotmc.org/javadocs/bukkit/org/bukkit/entity/EntityType.html')
  = attribute_row do
    = attribute_name('Item')
    = attribute_desc(t('modules.checks.variables.item-desc'))
    = attribute_desc(label('Item')) do
      = link("Item Attributes", "/modules/loadouts")
  = attribute_row do
    = attribute_name('Location')
    = attribute_desc(t('modules.checks.variables.location-desc'))
    = attribute_type(:xyz)
  = attribute_row do
    = attribute_name('Material')
    = attribute_desc(t('modules.checks.variables.material-desc'))
    = attribute_desc(label('Material')) do
      = link_to("Material Types", "https://hub.spigotmc.org/javadocs/bukkit/org/bukkit/Material.html")
  = attribute_row do
    = attribute_name('Player')
    = attribute_desc(t('modules.checks.variables.player-desc'))
    = attribute_type(:player)
  = attribute_row do
    = attribute_name('Spawn Reason')
    = attribute_desc(t('modules.checks.variables.spawn-desc'))
    = attribute_desc(label('Spawn Reason')) do
      = link_to("Spawn Reason Types", "https://hub.spigotmc.org/javadocs/bukkit/org/bukkit/event/entity/CreatureSpawnEvent.SpawnReason.html")
  = attribute_row do
    = attribute_name('Victim')
    = attribute_desc(t('modules.checks.variables.victim-desc'))
    = attribute_type(:player)

= subtitle(t('modules.checks.static.static'))

%p
  = t('modules.checks.static.text')

= attribute_table(t('modules.checks.check'), false, [t('table.type'), t('table.description')]) do
  = attribute_row do
    = attribute_column do
      :xmlcode
        <always>
    = attribute_desc(t('modules.checks.static.always-desc'))
  = attribute_row do
    = attribute_column do
      :xmlcode
        <never>
    = attribute_desc(t('modules.checks.static.never-desc'))



= subtitle(t('modules.checks.types.types'))

%p
  = t('modules.checks.types.text')

= attribute_table(t('modules.checks.check'), false, [t('table.type'), t('table.description')]) do
  = attribute_row do
    = attribute_column do
      :xmlcode
        <attacker>
    = attribute_column do
      = t('modules.checks.types.attacker-desc')
      :xml
        <attacker>
          <any>
            <team>red</team>
            <team>blue</team>
          </any>
        </attacker>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <carrying>
    = attribute_column do
      = t('modules.checks.types.carrying-desc')
      :xml
        <!-- Player must have a stack of exactly 3 diamonds -->
        <carrying material="diamond" amount="3"/>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <damage>
    = attribute_column do
      = t('modules.checks.types.damage-desc')
      :xml
        <damage>fall</damage>
    = attribute_column
  = attribute_row do
    = attribute_column do
      :xmlcode
        <entity>
    = attribute_column do
      = t('modules.checks.types.entity-desc')
      :xml
        <entity>pig</entity>
    = attribute_column
  = attribute_row do
    = attribute_column do
      :xmlcode
        <holding>
    = attribute_column do
      = t('modules.checks.types.holding-desc')
      :xml
        <holding material="stone sword"/>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <inside>
    = attribute_column do
      = t('modules.checks.types.inside-desc')
      :xml
        <inside region="red-base"/>
      :xml
        <inside>
          <region>
            <sphere origin="41, 15, 0" radius="5"/>
          </region>
        </inside>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <item>
    = attribute_column do
      = t('modules.checks.types.item-desc')
      :xml
        <item material="wood" damage="1"/>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <material>
    = attribute_column do
      = t('modules.checks.types.material-desc')
      :xml
        <material>log:1</material>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <objective>
    = attribute_column do
      = t('modules.checks.types.objective-desc')
      :xml
        <objective>right-hill</objective>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <random>
    = attribute_column do
      = t('modules.checks.types.random-desc')
      :xml
        <!-- 10% chance of passing -->
        <random>0.1</random>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <sometimes>
    = attribute_column do
      = t('modules.checks.types.sometimes-desc')
      :xml
        <sometimes/>
    = attribute_column
  = attribute_row do
    = attribute_column do
      :xmlcode
        <spawn>
    = attribute_column do
      = t('modules.checks.types.spawn-desc')
      :xml
        <spawn>spawner egg</spawn>
      :xml
        <spawn>breeding</spawn>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <team>
    = attribute_column do
      = t('modules.checks.types.team-desc')
      :xml
        <team>blue</team>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <time>
    = attribute_column do
      = t('modules.checks.types.time-desc')
      :xml
        <!-- Time equals 5 minutes, 0 seconds -->
        <time>5m</time>
      :xml
        <!-- Time is less than 5 minutes -->
        <time compare="less than">5m</time>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <victim>
    = attribute_column do
      = t('modules.checks.types.victim-desc')
      :xml
        <victim>
          <any>
            <team>green</team>
            <team>yellow</team>
          </any>
        </victim>
    = attribute_column
  = attribute_row do
    = attribute_column do
      :xmlcode
        <wearing>
    = attribute_column do
      = t('modules.checks.types.wearing-desc')
      :xml
        <wearing material="iron chestplate"/>
  = attribute_row do
    = attribute_column do
      :xmlcode
        <weather>
    = attribute_column do
      = t('modules.checks.types.weather-desc')
      :xml
        <weather>clear</weather>
      :xml
        <weather>downfall</weather>


= subtitle(t('modules.checks.combinations.combinations'))

%p
  = t('modules.checks.combinations.text')

= attribute_table(t('modules.checks.check'), false, [t('table.type'), t('table.description')]) do
  = attribute_row do
    = attribute_column do
      :xmlcode
        <all>
    = attribute_desc(t('modules.checks.combinations.all-desc'))
  = attribute_row do
    = attribute_column do
      :xmlcode
        <any>
    = attribute_desc(t('modules.checks.combinations.any-desc'))

= subtitle(t('modules.checks.modifiers.modifiers'))

%p
  = t('modules.checks.modifiers.text')

= attribute_table(t('modules.checks.modifiers.modifier'), false, [t('table.type'), t('table.description')]) do
  = attribute_row do
    = attribute_column do
      :xmlcode
        <allow>
    = attribute_desc(t('modules.checks.modifiers.allow-desc'))
  = attribute_row do
    = attribute_column do
      :xmlcode
        <deny>
    = attribute_desc(t('modules.checks.modifiers.deny-desc'))
  = attribute_row do
    = attribute_column do
      :xmlcode
        <not>
    = attribute_desc(t('modules.checks.modifiers.not-desc'))


= subtitle(t('modules.checks.examples.examples'))
%p
=  t('modules.checks.examples.text')

%p
= coming_soon